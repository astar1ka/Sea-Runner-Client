export default function SelectAllice(props: any){
    const server = props.API;

    const onClickHandler = () => {
    }

    return <div className='selectAllice' onClick={onClickHandler}>
        <image></image>
        <a></a>
    </div>
}